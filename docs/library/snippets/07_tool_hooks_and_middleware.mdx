---
title: Snippet 07: Tool Hooks and Middleware
description: Add runtime middleware around LLM and tool paths.
---

```python
from afk.llms import LLMBuilder

client = (
    LLMBuilder()
    .provider("litellm")
    .model("gpt-4.1-mini")
    .profile("production")
    # .with_middleware(my_middleware)
    .build()
)
```

Use middleware to enforce per-request metadata, tracing, or additional validation.
